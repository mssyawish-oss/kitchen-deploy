# NEVER sync data or secrets — these live only on the server
kitchen_data.json
bbq_history.json
*.log
.DS_Store
backup/
BACKEND-UPDATE-PENDING.txt
deploy-status.json
backups/
kitchen_data.json
*.bak
# camera stills of customers/staff — NEVER commit to this PUBLIC repo
walkin_log/
task_photos/
